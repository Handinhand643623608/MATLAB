function u_colormap_EEG(colorData, channelLabels, colorBounds)
% F_EEG_COLORMAP Produce a spatial map of colored EEG electrodes
%   f_eeg_colormap produces a colored EEG spatial map based on the input
%   data. Red colors correspond to higher values of input data, while blue
%   colors correspond to lower values. This function only makes use of 64
%   electrodes. 
% 
%   Syntax:
%   f_EEG_colormap(colorData, channelLabels, colorBounds)
% 
%   COLOR_DATA: A vector of data of length 64, one value for each of the
%               electrodes being plotted (in the same order found in the
%               channel labels file). These data determine the colors seen
%               inside of each electrode on the map.
%   CHANNELLABELS: A cell array of channel labels (64 in total).
%   COLORBOUNDS: A two-element vector ([lower upper]) denoting the minimum
%                and maximum values being mapped to colors.
% 
%   Written by Josh Grooms on 4/19/2012
%       Modified on 5/29/2012 to include option for custom labels
%       Modified on 5/31/2012 to include option for subject 7+ channel labels
%       Modified on 6/4/2012 to include extra channel labels for subject 7+
%       Modified heavily on 7/27/2012 to work with new data structures

%% Initialize
% Load data stored elsewhere
load EEG_coordinates;

if isempty(colorBounds)
    colorBounds = [-1 1];
end

%% Produce the spatial maps
% Find the electrode spatial coordinates
for i = 1:length(channelLabels)
    electrodeCoordinates(i, :) = EEG_coordinates.(channelLabels{i});
end

% Plot the colormaps
figure;
f_textoval_color(electrodeCoordinates(:, 1)', electrodeCoordinates(:, 2)', channelLabels, colorData, colorBounds)
axis equal
axis off
set(gca, 'xtick', [], 'ytick', [])
colorbar